import { render } from '@testing-library/react'
import ProgressSpinner from '@components/ProgressSpinner'

describe('Test ProgressSpinner', () => {
  test('test render of ProgressSpinner', () => {
    render(<ProgressSpinner />)
  })
})
